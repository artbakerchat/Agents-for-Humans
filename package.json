{
  "name": "agents-for-humans",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:cloudflare": "node scripts/dev-cloudflare.mjs",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^6.1.0",
    "wrangler": "^4.129.0"
  },
  "dependencies": {
    "hls.js": "^1.7.2"
  },
  "allowScripts": {
    "workerd@1.20260903.1": true,
    "esbuild@0.28.1": true
  }
}
